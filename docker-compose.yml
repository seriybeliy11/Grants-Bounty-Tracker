version: '3'
services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    networks:
      - mynetwork

  parsers:
    build:
      context: ./parsers
    environment:
      - GITHUB_TOKEN=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
    volumes:
      - ./parsers:/app
    depends_on:
      - redis
    networks:
      - mynetwork

  frontend:
    build:
      context: ./frontend_new
    ports:
      - "5173:5173"
      - "3000:3000"
    depends_on:
      - parsers
    networks:
      - mynetwork

networks:
  mynetwork:
